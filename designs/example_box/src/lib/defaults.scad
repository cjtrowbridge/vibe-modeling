// Project defaults (all dimensions in mm)

// Part selector (set via -D part_id=0/1/2...)
// NOTE: Numeric ids are safer than strings across shells/OSes.
part_id = 0; // 0=example_box

// Outer shell size
box_x = 60;
box_y = 40;
box_z = 24;

// Hollowing / printability
wall = 2;
floor_thickness = 2;

// Optional features
center_hole_d = 0; // 0 disables the hole
