# Journal — 2026-08-28

## Field Ownership

- `Today's Intentions` and `Notes / Reflections` are user-only. Agents may
  insert only verbatim user-provided text and otherwise leave `-`.
- All other sections are agent-managed and evidence-bound.

## Today's Intentions

-

## Kickoff Context

- Date: 2026-08-28
- Journal file status: newly created (previous entries end at 2026-08-13)
- Relevant plans: `plans/current/2026-08-13-17-44-00_restore-angled-screen-2u-rails.md`
  (active; today's work continues its unresolved items 2.2 and 4.2)
- Missing artifacts created: this journal entry
- Session trigger: user requested a full review of the cyberdeck-2 design
  history, then reported that the 45-degree angled-screen rails render as
  solid blocks with no through-holes.

## Plan Checkpoint Linkage

- Active plan path: `plans/current/2026-08-13-17-44-00_restore-angled-screen-2u-rails.md`
- Checklist items updated: none (work paused before completion; see work log)
- Plan revisions proposed or approved: none (paused)

## Repo Work Log

- [2026-08-28 local] Reviewed the full cyberdeck-2 design history (journal
  2026-08-04 through 2026-08-13, plan files, `enclosure_blockout.scad`,
  `lib/rack_v2_0_0.scad`, `configs/rev_0001.json`).
  [reason: user requested a review of the design process history before
  debugging the solid-block rails]
  [follow-up: none]

- [2026-08-28 local] Root-caused the solid angled-screen rail blocks: CGAL
  silently fails to subtract the small 3.6 mm M3 cylinders from the polyhedron
  union once the frame contains 45-degree angled faces. The hole cuts in
  `shell_master()`'s `difference()` block were being dropped, leaving solid
  15.875 x 3.0 x 88.9 mm rail blocks.
  [reason: user reported "there are no holes through the angled rails"]
  [follow-up: none]

- [2026-08-28 local] Fixed the CGAL failure by pre-cutting the M3 holes from
  the simple box-union frame before it is unioned into the shell:
  `shell_master_uncut()` now uses `angled_screen_frame()` (difference of
  `angled_screen_frame_uncut()` and `angled_screen_insert_hole_cuts()`) and the
  duplicate `angled_screen_insert_hole_cuts()` call was removed from
  `shell_master()`'s `difference()` block. `angled_screen_nut_pocket_cuts()`
  remains in the shell difference.
  [reason: cutting cylinders from simple boxes before union avoids the
  polyhedron/45-degree-face boolean failure]
  [follow-up: user confirmed holes now appear in the rebuilt STLs]

- [2026-08-28 local] Built and validated: assembly contract validation passed,
  all 4 STLs regenerated in `output/cyberdeck-2/`, user visually confirmed
  the six M3 holes now pass through the angled rail faces.
  [reason: verify the CGAL fix end-to-end]
  [follow-up: two follow-on issues identified (see below)]

- [2026-08-28 local] Increased the hole-cut cylinder height from 3.04 mm
  (`screen_rack_face_rail_depth + 2*epsilon`, only 0.02 mm CGAL margin) to
  `screen_rack_face_rail_depth + 5.0` (8.0 mm) in
  `angled_screen_insert_hole_cuts()`. Discovered this is still insufficient:
  the holes must also clear the 8.8 mm per-hole nut lands, so the cylinder
  needs to reach at least `screen_rack_nut_land_depth + 5.0` (13.8 mm) to be a
  true through-hole. NOT yet applied; NOT rebuilt.
  [reason: 8.0 mm stop ~0.86 mm short of the nut-land back at Y=-8.8 mm]
  [follow-up: first action on resume]

- [2026-08-28 local] User reported the rails are "way too thick"; confirmed
  via multiple-choice that this means the front-to-back depth. Depth
  breakdown (face-local Y): rail face 3.0 mm (already at 3 mm minimum),
  `screen_rack_rear_clearance` = 50.8 mm side walls (dominant), and
  `screen_rack_nut_land_depth` = 8.8 mm nut lands (secondary). At 45 degrees
  the 50.8 mm projects ~35.9 mm horizontally, giving the blocky profile.
  [reason: user wants a slimmer angled-screen profile]
  [follow-up: reduce `screen_rack_rear_clearance` (candidate 25.4 or 31.75 mm)
  in `configs/rev_0001.json`, re-check all references and screen fit,
  re-validate contract, rebuild, user verify]

- [2026-08-28 local] WORK PAUSED by explicit user request. All pending work is
  captured in the follow-ups above. `git status`: `main...origin/main [ahead 4]`,
  dirty file `designs/cyberdeck-2/src/parts/enclosure_blockout.scad`
  (CGAL fix + 8.0 mm hole-cut change, uncommitted). No commit was made;
  commits require explicit user approval per host policy.
  [reason: user needs to step away and will resume later]
  [follow-up: resume order — (1) hole cylinder to
  `screen_rack_nut_land_depth + 5.0`; (2) reduce rear clearance in config;
  (3) optional nut-land depth reduction; (4) rebuild + audit; (5) user
  verify; (6) update plan/journal; (7) commit with approval]

## Notes / Reflections

-
