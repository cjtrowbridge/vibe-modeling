# Downtime Tasks

Downtime tasks are optional, analysis-only maintenance prompts. They may inspect
the repository but must write recommendations only to `downtime/reports/pending/`.
Suggested changes require a separate approved plan before implementation.
